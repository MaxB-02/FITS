version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Building static site for FITS application"
    build:
      commands:
        - echo "Static site build complete"
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
